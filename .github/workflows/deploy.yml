name: Deploy to Cloudflare Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      deployments: write
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Inject env vars into wrangler.json
        run: |
          node -e "
          const fs = require('fs');
          const cfg = JSON.parse(fs.readFileSync('wrangler.json','utf8'));
          cfg.vars = {};
          const keys = ['GROQ_API_KEY','CEREBRAS_API_KEY','GEMINI_API_KEY','NVIDIA_API_KEY','OPENROUTER_API_KEY','TAVILY_API_KEY','EXA_API_KEY','TURNSTILE_SECRET_KEY','DEV_IPS'];
          for (const k of keys) { if (process.env[k]) cfg.vars[k] = process.env[k]; }
          fs.writeFileSync('wrangler.json', JSON.stringify(cfg, null, 2));
          console.log('Injected', Object.keys(cfg.vars).length, 'env vars into wrangler.json');
          "

      - name: Create .env from secrets
        run: |
          cat > .env << 'ENVEOF'
          GROQ_API_KEY=${{ secrets.GROQ_API_KEY }}
          CEREBRAS_API_KEY=${{ secrets.CEREBRAS_API_KEY }}
          GEMINI_API_KEY=${{ secrets.GEMINI_API_KEY }}
          NVIDIA_API_KEY=${{ secrets.NVIDIA_API_KEY }}
          OPENROUTER_API_KEY=${{ secrets.OPENROUTER_API_KEY }}
          TAVILY_API_KEY=${{ secrets.TAVILY_API_KEY }}
          EXA_API_KEY=${{ secrets.EXA_API_KEY }}
          TURNSTILE_SECRET_KEY=${{ secrets.TURNSTILE_SECRET_KEY }}
          DEV_IPS=${{ secrets.DEV_IPS }}
          ENVEOF
          echo ".env created"

      - name: Build
        run: npm run build
        env:
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
          CEREBRAS_API_KEY: ${{ secrets.CEREBRAS_API_KEY }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          NVIDIA_API_KEY: ${{ secrets.NVIDIA_API_KEY }}
          OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}
          TAVILY_API_KEY: ${{ secrets.TAVILY_API_KEY }}
          EXA_API_KEY: ${{ secrets.EXA_API_KEY }}
          TURNSTILE_SECRET_KEY: ${{ secrets.TURNSTILE_SECRET_KEY }}
          DEV_IPS: ${{ secrets.DEV_IPS }}
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}

      - name: Post-process build (copy worker/chunks/middleware)
        run: |
          WORKER_ENTRY=$(ls dist/server/worker-entry-*.mjs 2>/dev/null | head -1)
          if [ -n "$WORKER_ENTRY" ]; then
            cp "$WORKER_ENTRY" dist/client/_worker.js
            echo "Copied $WORKER_ENTRY -> dist/client/_worker.js"
          elif [ -f "dist/server/entry.mjs" ]; then
            cp dist/server/entry.mjs dist/client/_worker.js
            echo "Copied dist/server/entry.mjs -> dist/client/_worker.js"
          fi

          if [ -d "dist/server/chunks" ]; then
            mkdir -p dist/client/chunks
            cp -r dist/server/chunks/* dist/client/chunks/
            echo "Copied server chunks"
          fi

          if [ -f "dist/server/virtual_astro_middleware.mjs" ]; then
            cp dist/server/virtual_astro_middleware.mjs dist/client/
            echo "Copied middleware"
          fi

          rm -f dist/client/.assetsignore
          rm -f dist/wrangler.json dist/server/wrangler.json 2>/dev/null || true
          rm -f .wrangler/deploy/config.json 2>/dev/null || true

      - name: Deploy to Cloudflare Pages
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          command: pages deploy dist/client --project-name tw-bot --branch main --commit-dirty=true

      - name: Set environment variables via API
        run: |
          node -e "
          const vars = {
            GROQ_API_KEY: process.env.GROQ_API_KEY,
            CEREBRAS_API_KEY: process.env.CEREBRAS_API_KEY,
            GEMINI_API_KEY: process.env.GEMINI_API_KEY,
            NVIDIA_API_KEY: process.env.NVIDIA_API_KEY,
            OPENROUTER_API_KEY: process.env.OPENROUTER_API_KEY,
            TAVILY_API_KEY: process.env.TAVILY_API_KEY,
            EXA_API_KEY: process.env.EXA_API_KEY,
            TURNSTILE_SECRET_KEY: process.env.TURNSTILE_SECRET_KEY,
            DEV_IPS: process.env.DEV_IPS,
          };
          const body = JSON.stringify({
            deployment_configs: { production: { env_vars: Object.fromEntries(
              Object.entries(vars).filter(([,v]) => v).map(([k,v]) => [k, {value: v}])
            )}}
          });
          const https = require('https');
          const req = https.request({
            hostname: 'api.cloudflare.com',
            path: '/client/v4/accounts/' + process.env.CLOUDFLARE_ACCOUNT_ID + '/pages/projects/tw-bot',
            method: 'PATCH',
            headers: { 'Authorization': 'Bearer ' + process.env.CLOUDFLARE_API_TOKEN, 'Content-Type': 'application/json' }
          }, (res) => { let d=''; res.on('data',c=>d+=c); res.on('end',()=>console.log(d)); });
          req.write(body); req.end();
          "
        env:
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
          CEREBRAS_API_KEY: ${{ secrets.CEREBRAS_API_KEY }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          NVIDIA_API_KEY: ${{ secrets.NVIDIA_API_KEY }}
          OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}
          TAVILY_API_KEY: ${{ secrets.TAVILY_API_KEY }}
          EXA_API_KEY: ${{ secrets.EXA_API_KEY }}
          TURNSTILE_SECRET_KEY: ${{ secrets.TURNSTILE_SECRET_KEY }}
          DEV_IPS: ${{ secrets.DEV_IPS }}
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}