# End-to-End Audit — 2026-07-11

Status: **IN PROGRESS** — methodology + first-hand findings recorded; the
multi-agent audit results and the fix log are appended below as work
proceeds. GitHub is the source of truth; every slice is committed.

## Objective (user request, 2026-07-11)

> improve diagram and presentation parsing and ingestion and overall
> performance and all end to end, improve all code, do a major end to end
> audit then strategize and improve all code, document and backup
> everything in github as you work

Scope interpreted as, in priority order:

1. **Diagram parsing/rendering** — artifact detection, mermaid/kroki
   normalization, renderer loading.
2. **Presentation (deck) + document parsing** — deck/doc JSON schemas,
   truncation salvage, exports (PDF/PPTX/DOCX).
3. **Ingestion** — PDF/DOCX extraction, chunking, embedding, RAG retrieval.
4. **Overall performance end-to-end** — streaming render path, server hot
   paths, client reactivity.
5. **Code quality** — dedupe, dead code, consistency; only where it reduces
   real risk or cost.

## Method

1. Baseline: `npm test` green — **265/265 across 48 files** before any
   change (2026-07-11).
2. Multi-agent audit (Claude Code workflow `tw-bot-e2e-audit`, run
   `wf_f110323a-01a`): 7 read-only finder agents, one per dimension
   (diagram-pipeline, deck-pipeline, ingestion-rag, stream-chat-e2e,
   client-perf, server-platform, code-quality), each returning max 14
   evidence-backed findings with file:line; then one **adversarial
   verifier agent per dimension** instructed to REFUTE each finding by
   re-reading the code. Only findings that survive verification
   (isReal && worthFixing) are implemented.
3. Fixes land in priority slices (bugs → perf → robustness → quality),
   `npm test` after each slice, one commit per coherent slice, push to
   `main` (deploy.yml auto-deploys).
4. Production smoke after deploy via `scripts/mobile-repro.mjs` pattern.

## First-hand pre-findings (main session, before workflow results)

Recorded so they can be cross-checked against the workflow output:

1. **[perf, streaming hot path]** `ChatMessages.svelte:68` re-runs
   `formatMarkdown(stripDisclaimers(content))` — a full `marked.parse` +
   `DOMPurify.sanitize` over the entire accumulated message — on every
   token batch (rAF-paced via `TokenBatcher`), and over **every** message
   whenever the array is replaced (`messages = [...messages, ...]`).
   Cost grows quadratically with response length during streaming.
   Candidate fix: cache parsed HTML per message keyed by content, and/or
   incremental block-level parsing (only re-parse the trailing block).
2. **[security, medium]** Kroki/mermaid SVG is sanitized by **regex only**
   (`sanitizeSvg`/`sanitizeHtml` in `renderer-loader.ts:512-525`,
   `kroki-renderer.ts:74-81`) and then injected via `{@html}` /
   `innerHTML` (`ArtifactPanel.svelte:249-253`, `ChatArtifactChip.svelte:48`,
   `renderer-loader.ts:315,500`). Regex HTML sanitization is bypassable
   (entity-encoded `javascript:`, nested tags). DOMPurify is already a
   dependency — use `DOMPurify.sanitize(svg, { USE_PROFILES: { svg: true, svgFilters: true } })`
   for the non-iframe SVG paths. (HTML artifacts already render in a
   `sandbox=""` iframe — that path is fine as defense-in-depth.)
3. **[quality, dead code]** `chunkText` in `embed-pipeline.ts:104` (blind
   500/100 slicing) is superseded by the paragraph-aware `chunkDocument`
   in `rag-client.ts` and is only referenced by a test mock. Candidate
   removal (with the stale "~700-char chunks" comment above `BATCH_SIZE`
   pointing at the real chunker).
4. **[robustness, low]** `TokenBatcher` uses `requestAnimationFrame` —
   in background tabs rAF is throttled/paused, so tokens buffer until the
   tab refocuses. Acceptable UX tradeoff, but worth a `setTimeout`
   fallback cap so background streams still make visible progress.

## Multi-agent audit results

_(appended when workflow `wf_f110323a-01a` completes)_

## Fix log

_(one entry per commit as slices land)_
